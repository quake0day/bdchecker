version: '3.8'

services:
  inspector:
    build: .
    container_name: disclosure-inspector
    restart: unless-stopped
    ports:
      - "8088:8080"
    volumes:
      - inspector-data:/data
    environment:
      - DB_PATH=/data/inspector.db
      - PORT=8080

volumes:
  inspector-data:
